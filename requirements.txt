# Claude Voice Interface — зависимости
# Установка: pip install -r requirements.txt

# Web сервер
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# STT — Whisper на GPU
faster-whisper>=1.1.0

# PyTorch (нужен для STT + XTTS)
# При установке с CUDA поддержкой используй команду ниже (НЕ через этот файл):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.3.0
torchaudio>=2.3.0

# Edge TTS — для генерации стандартных голосов (setup_voices.py, setup_board_voices.py)
edge-tts>=6.1.0

# XTTS v2 — TTS for all modes (Step/Live + Board voices)
TTS>=0.22.0

# HTTP клиент (Board of Directors — OpenRouter)
httpx>=0.27.0

# Board Memory (persistence layer)
aiosqlite>=0.19.0
qdrant-client>=1.7.0           # optional — falls back to SQLite LIKE if missing
sentence-transformers>=2.2.0   # optional — local embeddings for Qdrant

# Утилиты
pydantic>=2.0.0
websockets>=12.0

# Telegram Bridge (file receiver)
python-telegram-bot>=20.0
httpx>=0.27.0

# Marketing Factory
fpdf2>=2.7.9
